name: Validate Estimate Sale Fast Entry

on:
  pull_request:
    branches:
      - main
    paths:
      - ".github/workflows/validate-estimate-sale-fast-entry.yml"
      - "package.json"
      - "packages/ui/**"
      - "apps/web/**"

permissions:
  contents: read

concurrency:
  group: validate-estimate-sale-fast-entry-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - name: Check out pull request merge ref
        uses: actions/checkout@v4

      - name: Use Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma client
        run: npm run db:generate

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npm run typecheck

      - name: Test
        run: npm test

      - name: Build
        run: npm run build --workspace @trustfirst/web

      - name: Verify clean Estimate source
        shell: bash
        run: |
          set -euo pipefail
          test -f apps/web/src/components/hardware/estimate-bill-form.tsx
          test -f 'apps/web/src/app/(platform)/admin/hardware/quotations/[documentId]/edit/page.tsx'
          test -f 'apps/web/src/app/api/hardware/trade/[documentId]/estimate/route.ts'
          test ! -e scripts/.estimate-sale-fast-entry
          test ! -e scripts/.estimate-sale-fast-entry-v2
          test ! -e scripts/run_estimate_sale_fast_entry.py
          test ! -e scripts/run_estimate_sale_fast_entry_v2.py
          test ! -e scripts/fix_estimate_sale_types.py
          git diff --check
          echo ESTIMATE_SALE_FAST_ENTRY_VALIDATION_SUCCESSFUL
