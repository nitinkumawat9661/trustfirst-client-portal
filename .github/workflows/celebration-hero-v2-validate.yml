name: Validate Celebration Hero V2

on:
  push:
    branches:
      - sync/celebration-premium-affordable-hero

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: celebration-site
    steps:
      - name: Check out exact revision
        uses: actions/checkout@v5
      - name: Install and validate
        run: |
          set -euo pipefail
          npm ci
          npm audit --omit=dev
          npm run audit
          npm run typecheck
          npm run build
